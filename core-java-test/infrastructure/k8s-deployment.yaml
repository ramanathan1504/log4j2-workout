# ⚠️  This file is superseded. Use k8s-job.yaml instead.
#
# core-java-test is a one-shot test runner — a Kubernetes Job is the correct
# resource type (not a Deployment).
#
# Apply the correct manifest:
#   kubectl apply -f core-java-test/infrastructure/k8s-job.yaml
#
# Watch output:
#   kubectl logs -f job/core-java-log4j-test
