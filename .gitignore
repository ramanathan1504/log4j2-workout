# Build output
target/
build/
out/
.gradle/

# Bench runtime output
logs/
.bench/
repros/**/*.zip
# What the containers write back to the host. infra/output/syslog/received.log
# is how the syslog appender is verified — it is evidence of a run, not source,
# and it was committed once by a careless `git add -A`.
infra/output/

# Large agents / archives
*.jar
!**/gradle/wrapper/gradle-wrapper.jar
*.zip

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
Thumbs.db
__pycache__/
