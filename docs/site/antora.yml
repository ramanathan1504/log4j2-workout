# Antora component descriptor.
#
# The same layout Log4j's own documentation uses (src/site/antora in
# logging-log4j2), so these pages can be dropped into an Antora playbook
# without rearranging anything.
name: log4j-bench
title: Log4j Bench
version: ~
start_page: index.adoc
nav:
  - modules/ROOT/nav.adoc
