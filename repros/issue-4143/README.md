# Log4j #4143 — throwable rendering under JsonTemplateLayout

https://github.com/apache/logging-log4j2/issues/4143

Generated by `./bench repro 4143` from the Log4j feature bench.

## Reproduction

```bash
unzip log4j-issue-4143-repro.zip
cd log4j-issue-4143-repro
./run.sh 2.26.1     # or any version
```

The project is standalone — it depends only on `log4j-api` and `log4j-core`,
with no parent POM and no reference to the bench it came from.

## Verification matrix

| Log4j version | Result | Detail |
|---|:---:|---|
| `2.24.1` | ❌ FAIL | `java.lang.IllegalArgumentException: loop in causal chain` <br/><sub>swallowed by StatusLogger</sub> |
| `2.25.4` | ✅ PASS | clean run, no StatusLogger error |
| `2.26.1` | ✅ PASS | clean run, no StatusLogger error |
| `2.27.0-SNAPSHOT` | ✅ PASS | clean run, no StatusLogger error |

Full output per version is under `output/`.

## What it does

- Scenario: `exceptions` (`ExceptionScenario.java`)
- Configuration: `xml/layout-jsontemplate` → shipped as `src/main/resources/log4j2.xml`
- Entry point: `org.apache.logging.repro.Main`

Dependencies beyond `log4j-api` / `log4j-core`:

  - `log4j-layout-template-json` — required by the shipped configuration


## Environment

- Generated: 2026-08-03 17:38:05Z
- JDK: openjdk version "17.0.14" 2025-01-21
- Maven: Apache Maven 3.9.12 (848fbb4bf2d427b72bdb2471c22fced7ebd9a7a1)
