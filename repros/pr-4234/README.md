# Log4j pr #4234 reproduction

https://github.com/apache/logging-log4j2/pull/4234

Generated by `./bench repro 4234` from the Log4j feature bench.

## Reproduction

```bash
unzip log4j-pr-4234-repro.zip
cd log4j-pr-4234-repro
./run.sh 2.26.1     # or any version
```

The project is standalone — it depends only on `log4j-api` and `log4j-core`,
with no parent POM and no reference to the bench it came from.

## Verification matrix

| Log4j version | Result | Detail |
|---|:---:|---|
| `2.24.1` | ✅ PASS | clean run, no StatusLogger error |
| `2.25.5` | ✅ PASS | clean run, no StatusLogger error |
| `2.26.0` | ✅ PASS | clean run, no StatusLogger error |
| `2.26.1` | ✅ PASS | clean run, no StatusLogger error |

Full output per version is under `output/`.

## What it does

- Scenario: `rollover` (`RolloverScenario.java`)
- Configuration: `xml/repro-zstd-level` → shipped as `src/main/resources/log4j2.xml`
- Entry point: `org.apache.logging.repro.Main`

Dependencies beyond `log4j-api` / `log4j-core`:

  - `commons-compress` — required by the shipped configuration
  - `zstd-jni` — required by the shipped configuration


## Environment

- Generated: 2026-08-09 07:01:32Z
- JDK: openjdk version "17.0.14" 2025-01-21
- Maven: Apache Maven 3.9.12 (848fbb4bf2d427b72bdb2471c22fced7ebd9a7a1)
