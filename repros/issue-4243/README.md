# Log4j issue #4243 reproduction

https://github.com/apache/logging-log4j2/issues/4243

Generated by `./bench repro 4243` from the Log4j feature bench.

## Reproduction

```bash
unzip log4j-issue-4243-repro.zip
cd log4j-issue-4243-repro
./run.sh 2.26.1     # or any version
```

The project is standalone — it depends only on `log4j-api` and `log4j-core`,
with no parent POM and no reference to the bench it came from.

## Verification matrix

| Log4j version | Result | Detail |
|---|:---:|---|
| `2.24.1` | ❌ FAIL | `java.lang.NullPointerException: Cannot read the array length because ` <br/><sub>swallowed by StatusLogger</sub> |
| `2.26.1` | ❌ FAIL | `StatusLogger reported an appender error` <br/><sub>swallowed by StatusLogger</sub> |

Full output per version is under `output/`.

## What it does

- Scenario: `messages` (`MessageScenario.java`)
- Configuration: `xml/repro-csv-npe` → shipped as `src/main/resources/log4j2.xml`
- Entry point: `org.apache.logging.repro.Main`

Dependencies beyond `log4j-api` / `log4j-core`:

  - `commons-csv` — required by the shipped configuration


## Environment

- Generated: 2026-08-05 23:08:54Z
- JDK: openjdk version "17.0.14" 2025-01-21
- Maven: Apache Maven 3.9.12 (848fbb4bf2d427b72bdb2471c22fced7ebd9a7a1)
